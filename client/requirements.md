## Packages
date-fns | Formatting dates for blog posts and tables
lucide-react | Standard icon set for services and admin UI

## Notes
- Theme is based on a professional Nigerian Green and White aesthetic.
- Unauthenticated users trying to access `/admin/*` will be redirected to `/api/login`.
- Replit Auth is used; login is handled entirely by navigating to `/api/login`.
